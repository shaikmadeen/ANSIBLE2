- name: install packages
  hosts: mongodb
  become: yes
  tasks:
  - name: install package
    ansible.builtin.yum:
     msg: "{{item}}"
     state: installed
    loop:
    - nginx
    - git
    - postfix 

    