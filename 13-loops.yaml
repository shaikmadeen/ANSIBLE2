- name: install packages
  hosts: localhost
  become: yes
  tasks:
  - name: install package
    ansible.builtin.yum:
     msg: "{{item}}"
     state: installed
    loop:
    - nginx
    - git
    - postfix 

    